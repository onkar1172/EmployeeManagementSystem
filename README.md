# EmployeeManagementSystem
The Employee Management System is a web application built with Spring Boot, Thymeleaf, and Spring Data JPA. It manages employee and department records through CRUD operations using the MVC architecture. The system implements a One-to-Many relationship between Department and Employee using JPA entity mapping.
